import streamlit as st
import pandas as pd
import numpy as np
# from data.create_data import create_table

def app():
    
    st.title('Multipage Streamlit Stock Dashboard')
    st.markdown("### This is a multipage dashboard built to display the technical indicators and streaming social sentiment with Streamlit. ")
    

    # st.markdown("### Approach")
    # df = create_table()
    # st.write(df)

    # st.write('1. Create the app.py ')
    # st.write('1. Create the app.py ')
    # st.write('1. Create the app.py ')
    # st.write('1. Create the app.py ')


    st.write("Check `README.md` for references / resources used to consolidate building this dashboard.")
    
    st.markdown("### How to get started")

    st.write("`Clone/Download` this repo to use for your projects")